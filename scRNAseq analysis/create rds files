#script to create and merge RDS files for analysis
#Create RDS files for each patient and condition, and merge them for pools A and B.
#faut créer un fichier rds par patient et conditions et les fusioner ensuite pour pool a et b et essayer de fusioner les 2 pool ensuite
library(Seurat)
library(dplyr)
library(ggplot2)

data <- Read10X(data.dir = "data/") # chemin vers la data
seu <- CreateSeuratObject(
  counts = data,
  project = "AML",
  min.cells = 3,
  min.features = 200
)
